spring:
  redis:
    host: localhost
    database: 0
    port: 6379
    timeout: 3000
    lettuce:
      pool:
        max-active: 8
        max-wait: 3000
        max-idle: 8
        min-idle: 0
    jedis:
      pool:
        max-active: 8
        max-wait: 3000
        max-idle: 8
        min-idle: 0

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
bloom:
    filter:
      expectedInsertions: 1000
      fpp: 0.001F

